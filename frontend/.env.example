# Copy this file to .env and adjust if your backend runs on a different host/port.
# .env is gitignored — never commit real values.

# Base URL for the backend API. In development the Vite proxy handles this so
# you can leave it blank and requests will go through the proxy at /api/...
# In production builds, set this to the absolute backend URL.
VITE_API_BASE_URL=
